.container
  .row
    - @settings.each do |setting|
      = define_setting_action(setting)
  .row
    =link_to restart_nginx_server_admin_index_path do
      %button{:type => "button", :class => "btn btn-warning", :id => "restart_server_btn"}
        Restart server
      / #restarting_loader
      /   =image_tag "/assets/ajax-loader.gif"
  = render '/modals/server_loader'
